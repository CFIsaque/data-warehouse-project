--Bronze

CREATE SCHEMA IF NOT EXISTS bronze
OPTIONS (
  location = "US"
);

--Prata
CREATE SCHEMA IF NOT EXISTS prata
OPTIONS (
  location = "US"
);

CREATE SCHEMA IF NOT EXISTS ouro
OPTIONS (
  location = "US"
);
